spring:
  application:
    name: HabitTrack

  datasource:
    url: jdbc:postgresql://localhost:5432/habitTracker
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true

  flyway:
    enable: true
